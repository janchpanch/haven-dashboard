# Dashboard Enabled Analytics for Transaction Data

Given an example dataset in `./src/dataset.json`, parse the data and display it using a React dashboard.

# Running This Project
Clone the repo, run `npm install`, then `npm run dev`.
